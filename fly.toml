app = "ciphertalker"
primary_region = "jed"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
```

Delete everything else in that file including the backticks and the Jeddah comment line. Save and commit, then pull the changes:
```
git pull
fly launch
