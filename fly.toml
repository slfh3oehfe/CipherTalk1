app = "ciphertalker"
primary_region = "jed"

[build]
  builder = "paketobuildpacks/builder:base"

[env]
  PORT = "8080"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
```

`jed` = Jeddah, Saudi Arabia — the closest region to you.

**4. Add a `Procfile`**
```
web: gunicorn app:app --bind 0.0.0.0:8080
```

**5. Deploy**
```
fly launch
fly deploy
```

**6. Persistent storage for the database**
```
fly volumes create data --size 1
